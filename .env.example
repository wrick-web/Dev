# Database URL (SQLite)
# For local development, use a file path
DATABASE_URL="file:./prisma/dev.db"

# For production with Neon PostgreSQL (optional)
# DATABASE_URL="postgresql://user:password@host/database"
